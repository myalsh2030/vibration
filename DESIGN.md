# مواصفات موقع «أساسيات ميكانيكا الموائع» التدريبي

## الجمهور والنبرة
- المتدربون: فنيو صيانة ميكانيكية بالكلية التقنية (ليسوا مهندسين).
- **ممنوع**: الاستنتاجات الرياضية، البراهين، التفاضل والتكامل.
- **مسموح**: الحد الأدنى من المعادلات الجاهزة للاستخدام (P = ρ·g·h مثلًا) مع شرح كل رمز بالعربية ووحدته.
- النبرة: تشويقية، عملية، مرتبطة بواقع الورشة والصيانة (مضخات، ضواغط، مراوح، توربينات).
- اللغة: عربية، مع إظهار المصطلح الإنجليزي بجانب كل مفهوم تقني: `<span class="term">المضخة <i>Pump</i></span>`.
- الأسلوب: تلعيب Gamification كامل — نقاط XP، مستويات، أوسمة، مهام استكشاف.

## البنية التقنية
- موقع ثابت بدون أدوات بناء: HTML + CSS + ES Modules. يعمل على GitHub Pages.
- SPA بتوجيه hash (`#/lesson/u1l1`). اتجاه RTL. تصميم Mobile-first.
- PWA: manifest + service worker (كاش كامل للعمل دون اتصال).
- التخزين محلي فقط: localStorage تحت المفتاح `fm.v1`.
- لا مكتبات خارجية إطلاقًا. الخط Cairo مضمّن محليًا في `fonts/`.

## هيكل الملفات
```
index.html            القشرة + شاشة البداية
css/main.css          نظام التصميم كاملًا
js/app.js             الإقلاع + الراوتر + التنقل السفلي
js/store.js           الحالة + localStorage + pubsub
js/game.js            XP + المستويات + الأوسمة + الأيام المتتالية
js/ui.js              مكوّنات مشتركة (توست، كونفيتي، مودال، بطاقات)
js/quiz.js            محرك الاختبارات (mc, tf)
js/personalize.js     منطق التشخيص → الخطة الشخصية
js/screens/*.js       شاشات: welcome, diag, home, unit, lesson, labs, glossary, me
js/sims/simkit.js     عدة المحاكاة (canvas + تحكمات لمس)
js/sims/drawkit.js    عدة لوحة الرسم للمقررات الهندسية: Board (paper/line/poly/circle/arc/ellipse/dot/hatch/dim)
                      + LS (أنماط الخطوط الاصطلاحية: متصل/متقطع/محور/إنشاء/قطع/تهشير) + iso/dimetric/boxFaces
                      — اختيارية، تُستخدم فوق simkit في أي مقرر يرسم مساقط أو قطاعات أو مجسمات.
                      تقرأ ألوانها من --c-paper/--c-graph/--c-lead/--c-lead2/--c-hidden/--c-center (عرّفها في css/main.css)
js/sims/labkit.js     عدة المختبر للمقررات العملية: شريط مراحل التجربة (PhaseBar) + جدول قراءات (DataTable) + إدخال حساب بمناطق دقة (CalcInput/accuracyZone) — اختيارية، تُستخدم مع simkit في محاكيات التجارب المعملية
js/sims/<simId>.js    وحدات المحاكاة
js/sims/registry.js   سجل المحاكيات
data/course.js        بنية المقرر + بطاقات المحتوى
data/quizzes.js       بنوك الأسئلة
data/glossary.js      قائمة المصطلحات الثنائية
data/glossary-figs.js أشكال SVG توضيحية للمصطلحات (المفتاح = en حرفيًا؛ viewBox 320×140، ألوان عبر متغيرات CSS فقط، بلا defs/id)
```

## مخطط بيانات المقرر — `data/course.js`
```js
export const COURSE = {
  title: 'أساسيات ميكانيكا الموائع',
  units: [{
    id: 'u1', title: '...', icon: '💧', color: '#22d3ee',
    tagline: 'جملة تشويقية قصيرة',
    lessons: [{
      id: 'u1l1', title: '...', minutes: 8,
      concepts: ['density','viscosity'],   // وسوم تربط أسئلة التشخيص بالدرس
      blocks: [ /* بطاقات بالترتيب */ ]
    }]
  }]
}
```

### أنواع البطاقات (blocks)
| النوع | الحقول | الوصف |
|---|---|---|
| `concept` | `{t:'concept', title, icon, html}` | بطاقة مفهوم قصيرة (3-5 أسطر كحد أقصى)، html بسيط: `<b> <ul> <li> <span class="term">` |
| `figure` | `{t:'figure', svg, caption}` | رسم SVG توضيحي مضمّن (viewBox="0 0 400 260"، ألوان من متغيرات CSS مثل `var(--c-water)`) |
| `formula` | `{t:'formula', name, expr, terms:[{sym,ar,unit}], note}` | معادلة واحدة جاهزة، expr بصيغة HTML مثل `P = ρ × g × h` مع `<sub>` عند الحاجة |
| `example` | `{t:'example', title, given:[...], steps:[...], answer}` | مثال محلول مبسّط بأرقام سهلة، 3 خطوات كحد أقصى |
| `tip` | `{t:'tip', html}` | ربط بواقع الصيانة: «في الورشة سترى هذا عندما...» |
| `flip` | `{t:'flip', title, cards:[{front, back}]}` | بطاقات قلب تفاعلية (2-4 بطاقات) |
| `match` | `{t:'match', title, pairs:[{a, b}]}` | توصيل عناصر (3-5 أزواج) |
| `order` | `{t:'order', title, items:[...]}` | ترتيب خطوات (3-5 عناصر بالترتيب الصحيح) |
| `sim` | `{t:'sim', sim:'simId', title, desc, missions:[{id, text}]}` | محاكاة مضمّنة بمهام استكشاف |
| `quiz` | `{t:'quiz', ref:'quizId'}` | اختبار نقطة تفتيش نهاية الدرس |

قواعد المحتوى:
- كل درس: 6–12 بطاقة. يبدأ بـ concept تشويقي، وينتهي بـ quiz.
- نشاط تفاعلي واحد على الأقل (flip/match/order/sim) لكل درس.
- بطاقة tip واحدة على الأقل لكل درس تربط بالصيانة.
- المعادلات: فقط الضرورية، بدون اشتقاق. الأمثلة المحلولة بأرقام مبسطة.

## مخطط الاختبارات — `data/quizzes.js`
```js
export const QUIZZES = {
  diag:   { title:'...', questions:[...] },   // 12 سؤالًا تشخيصيًا (3 لكل وحدة)
  u1pre:  { title:'...', questions:[...] },   // 5-6 أسئلة قبلية للوحدة
  u1l1check: { title:'...', questions:[...] },// 3-4 أسئلة نقطة تفتيش للدرس
}
```
أنواع الأسئلة:
- `{t:'mc', q, opts:['..','..','..','..'], correct:0, why:'شرح قصير', unit:'u1', concept:'density'}`
- `{t:'tf', q, correct:true, why, unit, concept}`

قواعد: `why` إلزامي (يُعرض بعد الإجابة تعلّمًا لا عقابًا). `concept` يطابق وسوم دروس `course.js`. أسئلة عملية من واقع الصيانة قدر الإمكان، صياغة واضحة، بدون حسابات معقدة (حساب ذهني بسيط مقبول).

**قاعدة منع تسريب الإجابة بالطول (إلزامية):** لا يجوز أن تكون الإجابة الصحيحة قابلة للاصطياد من طولها أو أسلوبها. الخيارات الأربعة متقاربة الطول (±30%)، والمشتتات بنفس الجدية والنبرة التقنية (معقولة الشكل، خاطئة يقينًا). على مستوى الملف: الصحيحة هي الأطول في ≤25% من الأسئلة والأقصر في ≥15%. للتحقق: سكربت قياس يفحص النسب قبل أي دمج محتوى جديد.

## واجهة المحاكاة — `js/sims/`
كل محاكاة وحدة مستقلة:
```js
// js/sims/hydrostatic.js
import { SimKit } from './simkit.js'
export function mount(container, ctx) {
  // ctx.completeMission(missionId)  ← استدعِها عند تحقق شرط المهمة
  // ctx.missions ← قائمة المهام المعرفة في block
  // أنشئ عناصر التحكم عبر SimKit (منزلقات، أزرار) + canvas
  return { destroy() {...} }  // إيقاف RAF وإزالة المستمعات
}
```
قواعد المحاكاة:
- تعمل باللمس والفأرة، سريعة على جوال متواضع (لا جسيمات كثيفة).
- أرقام مباشرة تتغير أمام المتدرب مع تحريك المنزلق.
- المهام قابلة للتحقق برمجيًا (مثال: «ارفع العمق حتى يتجاوز الضغط 200kPa» → تحقق في حلقة الرسم).
- الألوان من لوحة التصميم (انظر أدناه)، النصوص عربية.

## الإيموجي: اختبرها قبل اعتمادها

خطوط ويندوز الشائعة (Segoe UI Emoji) لا ترسم إيموجي Unicode 11 فما فوق، فتظهر **مربعات فارغة**
أمام المتدرب. كلّفت هذه المسألة مقرر «الرسم الهندسي» جولة إصلاح كاملة لـ١٧ رمزًا.

**غير مدعومة (تجنّبها):** 🧭 🧊 🧠 🧩 🧮 🧰 🧱 🧾 🩻 🪚 🪜 🪟 🪢 🥏 🟠 🟡 🟢 🫧

**بديل مضمون:** 📐 📏 ✏️ 📑 📄 📋 🔷 🔶 🔸 ⚪ ⚫ ⬛ ⬜ 💡 🔍 🛠 🎯 🔒 🔥 ⭐ ✅ ⚠ 🎓 👷 🚩 ⭕ 🔢 🔪 🎲 📦

**كيف تفحص مقررك كله دفعة واحدة** — في وحدة تحكم المتصفح على المنصة:
```js
const sp=document.createElement('span');
sp.style.cssText='position:fixed;left:-9999px;font-family:Cairo,"Segoe UI",Tahoma,sans-serif;font-size:32px';
document.body.appendChild(sp);
const w=c=>{sp.textContent=c;return sp.getBoundingClientRect().width;};
const tofu=w('￿');                       // عرض مربع «المحرف المفقود»
const list=[/* كل الإيموجي المستعملة في مقررك */];
console.log(list.filter(e=>Math.abs(w(e)-tofu)<0.6));   // غير المدعومة
```

## لوحة التصميم
- خلفية: `#0b1220`، أسطح: `#141e33`، حدود: `rgba(255,255,255,.08)`
- ماء/أساسي: `#38bdf8`، ثانوي: `#22d3ee`، طاقة/تمييز: `#fbbf24`
- نجاح: `#34d399`، خطأ: `#f87171`، أوسمة: `#a78bfa`
- نصوص: `#e2e8f0` أساسي، `#94a3b8` ثانوي
- خط Cairo: 400 عادي، 700 عناوين، 800 أرقام وشعارات
- زوايا 16px للبطاقات، أهداف لمس ≥ 44px

## التلعيب
- XP: نشاط تفاعلي +10، مهمة محاكاة +15، إجابة صحيحة +5، إتمام درس +20، إتمام وحدة +50، التشخيصي +30.
- المستويات: قطرة ماء 💧 0، مبتدئ الورشة 🔧 100، فني ناشئ ⚙️ 250، فني موائع 🌊 500، فني أول 🛠️ 900، خبير مضخات 🚀 1400، أسطورة الورشة 🏆 2000.
- التخصيص: التشخيصي يلوّن الوحدات (أولوية 🔥 / عادي / متمكن ✅)، والقبلي يرتب دروس الوحدة ويقترح «تحدي التجاوز» لما يتقنه المتدرب.
- لا قفل إجباري: كل المحتوى متاح، والترتيب المقترح إرشادي.
